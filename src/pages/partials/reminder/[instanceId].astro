---
const instanceId = Astro.params.instanceId;

const ACCESS_KEY = import.meta.env.ACCESS_KEY;

try {
  const res = await fetch(
    `http://localhost:7071/runtime/webhooks/durabletask/instances/${instanceId}/terminate?taskHub=TestHubName&connection=Storage&code=${ACCESS_KEY}`,
    {
      method: "POST",
    }
  );

  if (!res.ok) {
    throw new Error("Failed to terminate instance");
  }

  return new Response(null, { status: 200 });
} catch (error) {
  console.error(error);
  return;
}
---
